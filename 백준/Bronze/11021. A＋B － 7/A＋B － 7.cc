#include <bits/stdc++.h>

using namespace std;
int n,a,b;
int main()
{
    cin >> n;
    for(int i=0;i<n;i++){
        cin >> a >> b;
        cout<<"Case #"<<i+1<<": "<<a+b<<"\n";
    }
    return 0;
}
